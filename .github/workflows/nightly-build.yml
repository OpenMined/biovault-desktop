name: Nightly Build

on:
  schedule:
    - cron: "0 5 * * *"
  workflow_dispatch:

concurrency:
  group: nightly-build
  cancel-in-progress: false

jobs:
  nightly:
    name: Nightly dry-run release build
    uses: ./.github/workflows/release.yml
    with:
      dry_run: true
    secrets: inherit
